spring:
  application:
    name: czula-podroz-api
  profiles:
    default: local
  jpa:
    open-in-view: false
    hibernate:
      # Schematem zarządza Liquibase; Hibernate ma go tylko sprawdzać.
      ddl-auto: validate
    properties:
      hibernate:
        jdbc.time_zone: UTC
        # Ogranicza N+1 przy wczytywaniu planów wyjazdów.
        default_batch_fetch_size: 50
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
    # Bez telemetrii — start aplikacji nie ma powodu dzwonić na zewnątrz
    # (w zamkniętej sieci produkcyjnej to zbędne opóźnienie przy każdym starcie).
    analytics-enabled: false
    # Kontekst MUSI być podany jawnie w każdym profilu: Liquibase bez kontekstu
    # wykonuje wszystkie zmiany, także te oznaczone jako dane przykładowe.
    contexts: ${LIQUIBASE_CONTEXTS:prod}
  jackson:
    # Jackson 3 (Spring Boot 4) domyślnie serializuje daty jako ISO-8601,
    # więc wystarczy pominąć puste pola w odpowiedziach.
    default-property-inclusion: non_null
  threads:
    virtual:
      enabled: true

server:
  port: ${PORT:8080}
  error:
    include-message: never
    include-stacktrace: never
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

czula:
  frontend:
    base-url: ${FRONTEND_BASE_URL:http://localhost:3002}
  cors:
    # 3002 — strona sprzedażowa, 3003 — panel organizatorek.
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3002,http://localhost:3003}
  catalog:
    few-left-threshold: 3
  booking:
    # Ile czasu koszyk trzyma miejsce bez złożenia zamówienia.
    hold-duration: PT20M
    # Ile czasu na opłacenie złożonego zamówienia.
    payment-window: PT60M
    max-seats-per-item: 4
    expiry-scan-interval: PT1M
    unpaid-scan-interval: PT5M
  messaging:
    # `log` — wiadomości trafiają do logu (środowisko lokalne i testy).
    # `smtp` — prawdziwa wysyłka; dane serwera w `spring.mail.*`.
    provider: ${MESSAGING_PROVIDER:log}
    from-name: Czuła Podróż
    from-email: ${MESSAGING_FROM_EMAIL:kontakt@czulapodroz.pl}
  payments:
    provider: ${PAYMENTS_PROVIDER:mock}
    api-base-url: ${API_BASE_URL:http://localhost:8080}
    webhook-secret: ${PAYMENTS_WEBHOOK_SECRET:}
  security:
    jwt:
      secret: ${JWT_SECRET:}
      issuer: czula-podroz-api
      access-token-ttl: PT15M
      refresh-token-ttl: P14D

logging:
  level:
    pl.czulapodroz.api: INFO
